варианты, что можно допилить (в порядке полезности/интереса).

======================================================

Валидация и единый формат ошибок (почти как FastAPI)
Сейчас у тебя валидация ручная и ошибки упрощённые. Можно сделать:

нормальную schema validation (например, Zod),

единый error-handler middleware,

одинаковые ответы для 422/404/409.

Почему интересно: это “core backend skill” и сильно улучшает качество.

======================================================


Автогенерация OpenAPI из кода
Сейчас OpenAPI у тебя вручную. Это нормально для миграции, но можно сделать “как у FastAPI”:

генерить OpenAPI из схем/роутов (или хотя бы из схем),

чтобы Swagger всегда соответствовал реальности.

Почему интересно: хороший инженерный паттерн, меньше ручной работы.

======================================================


Простая фильтрация/поиск/сортировка (полезно для UI и API)
Добавить query params:

GET /tickets?status=open

GET /tickets?priority=high

GET /tickets?assignee=Dima

GET /tickets?sort=id&order=desc

Почему интересно: сразу ощущение “реального продукта”, плюс практика SQL.

======================================================


Пагинация

GET /tickets?limit=20&offset=0
или

page/perPage

Почему интересно: типичный production requirement.

======================================================

“История изменений” (audit log)
Сделать таблицу ticket_events:

кто/когда/что поменял (created/updated/deleted)

хранить diff (например, old/new JSON).

Почему интересно: это уже уровень “системного” мышления и очень классно смотрится.

======================================================

Migration/seed вместо “живой” базы в git
Сделать:

schema.sql

seed.sql (несколько тикетов)

команда npm run db:reset

Почему интересно: правильный workflow, удобно преподавателю/коллегам разворачивать.

======================================================

Мини-авторизация (API key) и роли
Например:

чтение всем,

удаление (Danger Zone) только с ключом.

Почему интересно: безопасность и middleware.

======================================================

максимум пользы за минимум изменений — бери пункт 3 (фильтры/сортировка) + пункт 6 (schema/seed/reset). Это сделает проект заметно “серьёзнее” и при этом не раздует код.



========================////////////==============================

# 📔 Дневник разработки: Этап Рефакторинга

> **Статус:** Выполнено ✅
> **Цель:** Переход от монолитной структуры к модульной архитектуре для повышения читаемости и безопасности кода.

---

### 🏗 1. Структура проекта (Модуляризация)
Код разделен на независимые модули, что соответствует принципу разделения ответственности (**Separation of Concerns**):

* `database.py`: Настройка **SQLAlchemy**, создание движка (`engine`) и базового класса `Base`.
* `models.py`: Описание таблиц базы данных. Реализована связь **One-to-Many** (Один пользователь — много тикетов).
* `schemas.py`: Описание моделей **Pydantic**. Используются для валидации данных, приходящих на сервер и уходящих с него.
* `main.py`: Точка входа в приложение. Здесь происходит запуск сервера и регистрация маршрутов (API Endpoints).



### 🛠 2. Оптимизация работы с БД
* **Dependency Injection:** Реализована функция-генератор `get_db`.
* **Управление сессиями:** Сессия БД теперь внедряется в эндпоинты через `Depends`. Это гарантирует автоматическое закрытие соединения после завершения запроса, предотвращая блокировки базы SQLite.

### 🔐 3. Подготовка системы безопасности
* **Hashing:** Интегрирована библиотека `passlib`. Пароли теперь хешируются по алгоритму **bcrypt**, что исключает хранение паролей в открытом виде.
* **JWT (JSON Web Tokens):** Добавлена библиотека `python-jose`. Подготовлена логика для генерации и проверки цифровых подписей (токенов).
* **auth.py**: Создан отдельный модуль, инкапсулирующий всю логику безопасности.



### 🌐 4. Фронтенд и статика
* **Организация:** Статические файлы (`index.html`, `app.js`, `styles.css`) вынесены в отдельную папку `frontend/`.
* **Пути:** В `main.py` настроено монтирование папки: `app.mount("/ui", StaticFiles(directory="frontend", ...))`.
* **UI Fix:** Добавлена inline-иконка (favicon) в HTML, чтобы устранить ошибку `404 Not Found` в консоли браузера.

---

**Следующий шаг:** Реализация эндпоинтов `/register` и `/token` для полноценной авторизации.



