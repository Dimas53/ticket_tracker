# Ticket Tracker API – Спецификация проекта

Учебный мини-проект на FastAPI: простой тикет-трекер (аналог упрощенной Jira / GitHub Issues).  
Цель — отработка REST-API, CRUD-операций и архитектуры клиент-сервер.

---

## Цели проекта

- Создать минимальный backend на FastAPI с CRUD для тикетов
- Автоматически генерировать Swagger UI документацию
- Реализовать отдельный клиентский скрипт на `requests`
- Показать реалистичный сценарий работы с API

Проект ориентирован на **локальный запуск**. При необходимости можно открыть доступ через ngrok.

---

## Технологический стек

**Язык и библиотеки:**
- Python 3.10+
- FastAPI — веб-фреймворк для создания API
- Uvicorn — ASGI-сервер для запуска FastAPI
- Requests — HTTP-клиент для работы с API
- Pydantic — валидация данных (входит в FastAPI)

**Установка зависимостей:**
```bash
pip install fastapi "uvicorn[standard]" requests
```

---

## Структура проекта

```
project/
├── fast_api.py          # Backend приложение на FastAPI
├── client.py            # Клиентский скрипт на requests
├── requirements.txt     # Список зависимостей (опционально)
└── TICKET_TRACKER_SPEC.md  # Этот файл
```

### fast_api.py
Backend-приложение, содержит:
- Объявление приложения `app = FastAPI()`
- Pydantic-модель тикета
- In-memory базу данных (`dict`)
- CRUD эндпоинты
- Опционально: блок запуска через uvicorn

### client.py
Клиентский скрипт, демонстрирующий:
- Создание тикетов через API
- Чтение одного/всех тикетов
- Обновление статуса
- Удаление тикета
- Обработку ошибок (404)

### requirements.txt
```
fastapi
uvicorn[standard]
requests
```

---

## Модель данных: Ticket

**Структура тикета:**
```python
class Ticket(BaseModel):
    id: int
    title: str
    description: str
    status: str      # "open" | "in_progress" | "done"
    priority: str    # "low" | "normal" | "high"
    assignee: str    # например, "Dima"
```

**In-memory хранилище:**
```python
DB: dict[int, Ticket] = {}
```
Ключ — `id` тикета, значение — объект `Ticket`.

---

## API эндпоинты

**Базовый URL:** `http://127.0.0.1:8001`

### 1. Создать тикет
- **Метод:** `POST`
- **Путь:** `/tickets`
- **Описание:** Создает новый тикет в базе

**Пример запроса:**
```json
{
  "id": 1,
  "title": "Fix login bug",
  "description": "User cannot login with correct password",
  "status": "open",
  "priority": "high",
  "assignee": "Dima"
}
```

**Ответы:**
- `200` — тикет успешно создан
- `409` — тикет с таким `id` уже существует
- `422` — неверный формат данных (например, неправильный `status`)

---

### 2. Получить тикет по ID
- **Метод:** `GET`
- **Путь:** `/tickets/{ticket_id}`
- **Описание:** Возвращает информацию об одном тикете

**Пример:** `GET /tickets/1`

**Ответ:**
- `200` — JSON с данными тикета
- `404` — тикет не найден

---

### 3. Получить список всех тикетов
- **Метод:** `GET`
- **Путь:** `/tickets`
- **Описание:** Возвращает массив всех тикетов из базы

**Пример ответа:**
```json
[
  {
    "id": 1,
    "title": "Fix login bug",
    "description": "User cannot login with correct password",
    "status": "open",
    "priority": "high",
    "assignee": "Dima"
  },
  {
    "id": 2,
    "title": "Refactor auth module",
    "description": "Clean up legacy code",
    "status": "in_progress",
    "priority": "normal",
    "assignee": "Alex"
  }
]
```

**Ответ:** `200` — массив тикетов (может быть пустым)

---

### 4. Обновить тикет
- **Метод:** `PUT`
- **Путь:** `/tickets/{ticket_id}`
- **Описание:** Полное обновление всех полей тикета

**Пример запроса:**
```json
{
  "id": 1,
  "title": "Fix login bug (mobile)",
  "description": "Issue reproduced on mobile only",
  "status": "in_progress",
  "priority": "high",
  "assignee": "Dima"
}
```

**Ответ:**
- `200` — обновленный тикет
- `404` — тикет не найден
- `422` — неверный формат данных

**Примечание:** `id` в URL и в теле запроса должны совпадать.

---

### 5. Удалить тикет
- **Метод:** `DELETE`
- **Путь:** `/tickets/{ticket_id}`
- **Описание:** Удаляет тикет из базы

**Пример:** `DELETE /tickets/1`

**Ответ:**
- `200` — `{"message": "Ticket deleted successfully"}`
- `404` — тикет не найден

---

## Обработка ошибок и валидация

### Валидация полей
- `status` — разрешены только: `"open"`, `"in_progress"`, `"done"`
- `priority` — разрешены только: `"low"`, `"normal"`, `"high"`
- При передаче неверного значения — ответ `422 Unprocessable Entity`

### Дубликаты ID
- При попытке создать тикет с существующим `id` — ответ `409 Conflict`
- Рекомендация: в реальном проекте `id` генерируется автоматически

### Несуществующий тикет
- Все операции с несуществующим `id` возвращают `404 Not Found`

---

## Swagger и документация

FastAPI автоматически предоставляет:

- **Swagger UI:** `http://127.0.0.1:8001/docs`
- **ReDoc:** `http://127.0.0.1:8001/redoc`
- **OpenAPI JSON:** `http://127.0.0.1:8001/openapi.json`

Через Swagger UI можно:
- Визуально вызывать все эндпоинты
- Вводить JSON в тело запросов
- Просматривать ответы и коды статусов
- Использовать как простой UI для тестирования

---

## Запуск проекта

### 1. Подготовка окружения
###### В PyCharm settings выберите интерпретатор по пути /opt/homebrew/bin/python3.13
```bash
# Создать окружение (используя нативный Python 3.13 для M4)
python3 -m venv .venv

# Активировать виртуальное окружение
source .venv/bin/activate

# Установить зависимости
pip install fastapi "uvicorn[standard]" requests
```

### 2. Запуск backend (FastAPI)

**Через uvicorn (рекомендуется):**
```bash
uvicorn fast_api:app --reload --host 127.0.0.1 --port 8001
```
- `--reload` — авто-перезапуск при изменении кода

**Через Python (если добавлен блок запуска):**
```bash
python3 fast_api.py
```

### 3. Запуск клиента
```bash
python3 client.py
```

---

## Примеры кода

### Минимальный fast_api.py (скелет)
```python
from fastapi import FastAPI, HTTPException
from pydantic import BaseModel

app = FastAPI()

# Модель тикета
class Ticket(BaseModel):
    id: int
    title: str
    description: str
    status: str  # "open" | "in_progress" | "done"
    priority: str  # "low" | "normal" | "high"
    assignee: str

# In-memory база данных
DB: dict[int, Ticket] = {}

# Пример эндпоинта
@app.post("/tickets")
def create_ticket(ticket: Ticket):
    if ticket.id in DB:
        raise HTTPException(status_code=409, detail="Ticket already exists")
    DB[ticket.id] = ticket
    return ticket

@app.get("/tickets/{ticket_id}")
def get_ticket(ticket_id: int):
    if ticket_id not in DB:
        raise HTTPException(status_code=404, detail="Ticket not found")
    return DB[ticket_id]

# ... остальные эндпоинты
```

### Минимальный client.py (скелет)
```python
import requests

BASE_URL = "http://127.0.0.1:8001"

def create_ticket(ticket_data):
    response = requests.post(f"{BASE_URL}/tickets", json=ticket_data)
    return response.json()

def get_ticket(ticket_id):
    response = requests.get(f"{BASE_URL}/tickets/{ticket_id}")
    if response.status_code == 404:
        print("Ticket not found")
        return None
    return response.json()

# Пример использования
if __name__ == "__main__":
    # Создаем тикет
    new_ticket = {
        "id": 1,
        "title": "Fix login bug",
        "description": "User cannot login",
        "status": "open",
        "priority": "high",
        "assignee": "Dima"
    }
    result = create_ticket(new_ticket)
    print("Created:", result)
    
    # Получаем тикет
    ticket = get_ticket(1)
    print("Retrieved:", ticket)
```

---

## Сценарий работы client.py

1. Создать несколько тикетов (`POST /tickets`)
2. Получить один тикет по ID (`GET /tickets/{id}`)
3. Обновить статус тикета (`PUT /tickets/{id}`)
4. Получить список всех тикетов (`GET /tickets`)
5. Удалить тикет (`DELETE /tickets/{id}`)
6. Попытка получить удаленный тикет (ожидается `404`)

**Базовая конфигурация:**
```python
BASE_URL = "http://127.0.0.1:8001"
```



## Преимущества проекта

✅ **Реалистичность** — аналог рабочих инструментов (Jira, GitHub Issues)  
✅ **CRUD-операции** — полный цикл работы с данными  
✅ **Swagger** — автоматическая документация  
✅ **Расширяемость** — легко добавить новые функции  
✅ **Практичность** — можно использовать как реальный инструмент  

---

## Статусы и приоритеты

**Статусы:**
- `open` — новый тикет
- `in_progress` — в работе
- `done` — выполнен

**Приоритеты:**
- `low` — низкий
- `normal` — обычный
- `high` — высокий

---

**Версия:** 1.0  
**Дата:** Декабрь 2025  
**Автор:** Учебный проект


# 🚀 План развития Ticket Tracker: Auth, Roles & Pro Features

Этот план объединяет задачи по улучшению архитектуры, внедрению системы прав доступа (RBAC) и защите данных.

## 📌 Подготовка (Git Workflow)
- [ ] Сохранить текущее состояние: `git add .` -> `git commit -m "Base version before Auth"`
- [ ] Создать рабочую ветку для новых фич: `git checkout -b feature/auth-and-roles`

---

## 🛠 Этап 1: Архитектура и База данных (Core)
*Исправляем логические ошибки и готовим таблицы.*

- [ ] **Автоматический ID:** В `fast_api.py` изменить `id` в `TicketDB` на `autoincrement=True`.
- [ ] **Таблица пользователей:** Создать модель `UserDB` (id, username, password_hash, role).
- [ ] **Привязка тикетов:** Добавить в `TicketDB` поле `owner_id` (ForeignKey к пользователю).
- [ ] **Pydantic-схемы:** Разделить модели на `TicketCreate` (без ID) и `Ticket` (с ID).

---

## 🔐 Этап 2: Авторизация и Безопасность (Security)
*Реализация полноценного входа в систему.*

- [ ] **Хеширование паролей:** Интегрировать `passlib` (bcrypt), чтобы не хранить пароли в открытом виде.
- [ ] **JWT Tokens:** Реализовать эндпоинт `/token` для выдачи OAuth2 токенов.
- [ ] **Защита эндпоинтов:** Добавить `Depends(get_current_user)` во все CRUD операции.
- [ ] **Ролевая модель (User vs Admin):**
    - `User`: может редактировать/закрывать только **свои** тикеты.
    - `Admin`: имеет доступ к `Danger Zone` (удаление всего) и правке любых тикетов.

---

## 📊 Этап 3: Функционал и Фильтры (Pro Features)
*Делаем работу с данными удобной.*

- [ ] **Умный поиск:** Добавить Query параметры в `GET /tickets` (фильтр по статусу, приоритету).
- [ ] **Audit Log (История):** Создать таблицу `ticket_events`. 
    - Записывать: "Кто", "Когда", "Какое поле изменил" (например, статус: open -> closed).
- [ ] **Middleware:** Добавить логирование всех запросов к API в консоль или файл.

---

## 🎨 Этап 4: Интерфейс (Frontend)
*Отражаем изменения в браузере.*

- [ ] **Страница входа:** Добавить простую форму Login/Register в `index.html`.
- [ ] **Хранение сессии:** Сохранение JWT-токена в `localStorage`.
- [ ] **Умный UI:** - Скрывать кнопку "Удалить все" для обычных пользователей.
    - Показывать имя залогиненного пользователя в шапке.

---

## 📋 Итоговая матрица прав (RBAC)
| Действие | User (Обычный) | Admin (Админ) |
| :--- | :--- | :--- |
| Просмотр всех тикетов | ✅ Да | ✅ Да |
| Создание тикета | ✅ Да | ✅ Да |
| Редактировать свой тикет | ✅ Да | ✅ Да |
| Редактировать ЧУЖОЙ тикет | ❌ Нет | ✅ Да |
| Удалить ОДИН тикет | ⚠️ Только свой | ✅ Любой |
| Danger Zone (Удалить всё) | ❌ Нет | ✅ Да |





